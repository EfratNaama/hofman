# Demo Data Guide

## Methods
1. React demo loader page (recommended)
2. Node.js seed script
3. Firebase CLI JSON import

## Recommended Approach
Use a React loader page for simplicity and no CLI setup.

## Copilot Prompt
“Create a React component called LoadDemoData.jsx that uploads demo data to Firestore based on `/docs/05_data_schema_nosql.md`.”

## Cleanup
Delete the demo loader route before deployment.
